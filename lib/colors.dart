import 'package:flutter/material.dart';

final Color appBarColor = Colors.black54;
final Color backgroundColor = HSLColor.fromColor(Colors.indigo)
    .withSaturation(0.2)
    .withLightness(0.1)
    .toColor();
